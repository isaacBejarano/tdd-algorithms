function quickSortDESC(vector) {
	let left = [];
	let right = [];
	let pivot = vector[0];
	let output = vector; // else... def CASE BASE

	// if... RECURSION
	if (vector.length >= 2) {
		
	}


	return output;
}

module.exports = quickSort;
