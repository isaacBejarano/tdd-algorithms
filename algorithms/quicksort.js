function quickSort(vector) {
	let left = [];
	let right = [];
	let pivot = vector[0];
	let output = vector; // else... def CASE BASE

	// if... RECURSION
	if (vector.length >= 2) {}


	return vector;
}

module.exports = quickSort;
